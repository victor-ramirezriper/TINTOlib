
## Prerrequisitos

Asegúrate de tener instalado en tu sistema:
* **Python 3.12** (o una versión compatible).
* **Git**.
* Dependencias del sistema para **MPI** (necesarias para `mpi4py`). En Ubuntu/Debian puedes instalarlas con:
  ```bash
  sudo apt-get update && sudo apt-get install -y libopenmpi-dev openmpi-bin